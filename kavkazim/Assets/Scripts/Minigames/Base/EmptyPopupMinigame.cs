using UnityEngine;

namespace Minigames
{
    /// <summary>
    /// A simple minigame that displays an empty popup window.
    /// </summary>
    public class EmptyPopupMinigame : BaseMinigame
    {
        protected override void InitializeGameUI()
        {
            // Empty popup - no additional UI elements needed
            // The base class already creates the popup window structure
        }
    }
}

